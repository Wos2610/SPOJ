#include<iostream>
#include<string>
#include<stdbool.h>
#include<algorithm>
using namespace std;

string s[1000006];
int a[1000006] = {0};

int check(string str){
    int n = str.size();
    string temp = "";
    for(int i = 0; i < n; i++){
        if(str[i] != ','){
            temp += str[i];
        }
        else{
            if()
            temp = "";
        }
    }
}
int main(){
    int stt = 0;
    while(1){
        getline(cin, str);
    }
} 